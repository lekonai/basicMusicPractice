// this will contain essential html code such as the navigation bar
// just need to figure how it works though lol
const heading = document.createElement("h1");
const text = document.createTextNode("HELLO!");
heading.appendChild(text);
